export type Product = {
  productImage: string;
  productName: string;
  productBrand: string;
  productQuantity: string;
  isProductRequired: boolean;
}

export type User = {
  userName: string;
  userEmail: string;
  userPassword: string;
}